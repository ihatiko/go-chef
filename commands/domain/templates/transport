package {{.PackageName}}

import(
	"github.com/gofiber/fiber/v2"
)

type Handler interface {
    Create{{.FormattedFragmentName}}(fiber.Ctx) error
    Read{{.FormattedFragmentName}}(fiber.Ctx) error
    Update{{.FormattedFragmentName}}(fiber.Ctx) error
    Delete{{.FormattedFragmentName}}(fiber.Ctx) error
}