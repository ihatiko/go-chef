tree:
  domain-components:
    name: project1
  settings:
    project-path: /home/slava/GolandProjects/awesomeProject4
    project-name: awesomeProject4
    external-components:
      - database
      - log
  root-component:
    generated-files:
      - name: main
        extension: go
        template: main
      - name: .gitignore
        template: .gitignore
    nodes:
      - name: cmd
        generated-files:
          - name: cmd
            extension: go
            template: cmd
      - name: config
        generated-files:
          - name: config
            extension: go
            template: config
          - name: config
            extension: yml
            template: config-yml
      - name: internal
        nodes:
          - name: server
            generated-files:
              - name: server
                extension: go
                template: server
              - name: graceful-shutdown
                extension: go
                template: graceful-shutdown